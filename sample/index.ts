const array = [1, 2, 3];

array.map((x) => {
	return x + 1;
});

array.map(x => {
	return x + 1;
});

array.map(x => x + 1);

class SampleClass {
	public foo() {
		return 1;
	}

	private bar() {
		return 2;
	}
}
